# Data-binding map — an UNKNOWN type is treated as data (DBIND-03), so an
# empty source fails. Author must either give a source or relabel to a static
# type (e.g. label).

| Screen | Element     | Type | Source | Null/empty |
|--------|-------------|------|--------|------------|
| map    | status chip | chip |        | hide       |
