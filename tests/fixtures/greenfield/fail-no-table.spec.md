---
task: fail-no-table-fixture
visual: bundle
---

# Greenfield Spec — fail-no-table-fixture (fixture: fail-no-table)

Ghi chú: chưa có bảng hành vi nào được điền vào spec này.
TODO: liệt kê hành vi + Design-ref + Observable trước khi build (không có
bảng markdown nào ở dưới đây — chỉ có văn xuôi ghi chú, không có ký tự "|"
đầu dòng nào).

Đây là trạng thái spec bị bỏ trống nội dung bảng — phải bị chặn trước BUILD
vì không có cách nào chứng minh oracle 2 tầng đã sẵn sàng.
