# Legacy Behavior Manifest — /deal/example (fixture: fail-no-table)

Ghi chú: chưa có bảng hành vi nào được điền vào manifest này.
TODO: liệt kê hành vi + observable trước khi build (không có bảng markdown
nào ở dưới đây — chỉ có văn xuôi ghi chú, không có ký tự "|" đầu dòng nào).

Đây là trạng thái manifest bị bỏ trống nội dung bảng — phải bị chặn ở
P1->P3 vì không có cách nào chứng minh hành vi đã được port đúng.
